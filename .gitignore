# Derived from the local Anki install and the JMdict cache. Regenerated by
# `python3 pos.py` and `python3 vocab.py --refresh`; never committed, because
# they are a dump of one person's vocabulary rather than part of the project.
scripts/pos-table.json
scripts/.deck-words-cache.json

# Ichiran segmentation cache. Large, machine-local, rebuilt on demand.
scripts/.segcache/

__pycache__/
*.pyc
.DS_Store
